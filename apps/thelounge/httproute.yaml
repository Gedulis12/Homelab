---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: thelounge
spec:
  hostnames:
    - thelounge.g2net.xyz
  parentRefs:
    - name: cilium-shared-gateway
      namespace: kube-system
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: thelounge
          port: 80
